%{
#include "parser.tab.h"
#include <stdlib.h>
%}

%%

[0-9]+      {
               yylval.ival = atoi(yytext);
               return NUM;
            }

"+"         return PLUS;
"-"         return MINUS;
"*"         return MUL;
"/"         return DIV;
"("         return LPAREN;
")"         return RPAREN;

[ \t\n]+    ;

.           return yytext[0];

%%
int yywrap() {
    return 1;
}